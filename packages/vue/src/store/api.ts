import { ajax } from 'rxjs/ajax';

// export function createGroup(title: string) {
//   return ajax.post('/api/rooms', { title });
// }
// export function getRoomMessage(roomid: string) {
//   return ajax.get(`/api/rooms/${roomid}/messages`);
// }
