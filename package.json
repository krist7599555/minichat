{
  "name": "minichat",
  "private": true,
  "devDependencies": {
    "lerna": "^3.19.0"
  },
  "scripts": {
    "dev:front": "yarn --cwd ./packages/vue dev",
    "dev:back": "yarn --cwd ./packages/nest dev"
  }
}
